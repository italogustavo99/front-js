import {ClientConfig} from "./clientconfig"

export class Operators {
    name: String;
    login: String;
    password: String;
    email: String;
    isAdmin: Boolean;
    clientConfig: ClientConfig;
}